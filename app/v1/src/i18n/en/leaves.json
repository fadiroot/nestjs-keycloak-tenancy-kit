{
  "create": {
    "success": "Leave created successfully.",
    "error": "Failed to create leave.",
    "invalidLeaveDuration": "The leave duration is invalid."
  },
  "getAll": {
    "success": "Leaves retrieved successfully.",
    "error": "Failed to retrieve leaves."
  },
  "get": {
    "success": "Leave retrieved successfully.",
    "error": {
      "notFound": "Leave with ID {id} not found."
    }
  },
  "update": {
    "success": "Leave updated successfully.",
    "error": {
      "notFound": "Leave with ID {id} not found.",
      "duplicate": "Leave with the same data already exists.",
      "invalidLeaveDuration": "The leave duration is invalid."
    }
  },
  "delete": {
    "success": "Leave deleted successfully.",
    "error": {
      "notFound": "Leave with ID {id} not found.",
      "references": "Leave cannot be deleted due to existing references."
    }
  },
  "validation": {
    "leaveTypeId": {
      "required": "The leave type ID is required.",
      "uuid": "The leave type ID must be a valid UUID."
    },
    "userId": {
      "required": "The user ID is required.",
      "uuid": "The user ID must be a valid UUID."
    },
    "startDate": {
      "required": "The start date is required.",
      "format": "The start date must be in a valid date format (YYYY-MM-DD).",
      "invalid": "The start date is invalid.",
      "compareDates": "The start date must be before the end date."
    },
    "endDate": {
      "required": "The end date is required.",
      "format": "The end date must be in a valid date format (YYYY-MM-DD).",
      "invalid": "The end date is invalid.",
      "compareDates": "The end date must be after the start date."
    },
    "comment": {
      "maxLength": "The comment must be less than 255 characters."
    },
    "reason": {
      "maxLength": "The comment must be less than 255 characters."
    },
    "attachment": {
      "maxLength": "The attachment URL must be less than 255 characters."
    },
    "status": {
      "enum": "The status must be one of the predefined values."
    }
  }
}
