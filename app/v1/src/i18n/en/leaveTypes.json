{
  "create": {
    "success": "Leave type created successfully.",
    "error": "Failed to create leave type."
  },
  "getAll": {
    "success": "Leave types retrieved successfully.",
    "error": "Failed to retrieve leave types."
  },
  "get": {
    "success": "Leave type retrieved successfully.",
    "error": {
      "notFound": "Leave type with ID {id} not found."
    }
  },
  "update": {
    "success": "Leave type {name} updated successfully.",
    "error": {
      "notFound": "Leave type with ID {id} not found.",
      "duplicate": "Leave type name already exists."
    }
  },
  "delete": {
    "success": "Leave type {name} deleted successfully.",
    "default": "Default leave types cannot be deleted.",
    "error": {
      "notFound": "Leave type with ID {id} not found.",
      "references": "Leave type cannot be deleted due to existing references."
    }
  },
  "validation": {
    "name": {
      "required": "The leave type name is required.",
      "maxLength": "The leave type name must be less than 50 characters.",
      "IsNotEmpty": "The leave type name cannot be empty.",
      "alreadyExists": "A leave type with this name already exists."
    },
    "yearlyQuotaValue": {
      "required": "Yearly quota value is required.",
      "isNumber": "Yearly quota value must be a number.",
      "min": "Yearly quota value must be at least 1."
    },
    "yearlyQuotaUnit": {
      "required": "Yearly quota unit is required.",
      "isEnum": "Yearly quota unit must be one of the valid options."
    },
    "allowNegativeBalance": {
      "boolean": "The allow negative balance status must be a boolean."
    },
    "reasonRequired": {
      "boolean": "The reason required status must be a boolean."
    },
    "subtractWeekends": {
      "boolean": "The subtract weekends status must be a boolean."
    },
    "assignToType": {
      "required": "The assign to type is required.",
      "isEnum": "The assign to type must be one of the valid options (DEPARTMENTS or EMPLOYEES)."
    },
    "departments": {
      "required": "Departments are required when the assign to type is DEPARTMENTS.",
      "arrayNotEmpty": "The departments array cannot be empty.",
      "isUuid": "Each department ID must be a valid UUID."
    },
    "employees": {
      "required": "Employees are required when the assign to type is EMPLOYEES.",
      "arrayNotEmpty": "The employees array cannot be empty.",
      "isUuid": "Each employee ID must be a valid UUID."
    },
    "locations": {
      "required": "Locations are required.",
      "arrayNotEmpty": "The locations array cannot be empty.",
      "isUuid": "Each location ID must be a valid UUID."
    },
    "approvers": {
      "required": "Approvers are required.",
      "arrayNotEmpty": "The approvers array cannot be empty.",
      "isUuid": "Each approver ID must be a valid UUID."
    }
  }
}
